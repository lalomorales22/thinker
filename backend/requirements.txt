# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
websockets>=13.0
pydantic>=2.9

# Tinker fine-tuning SDK + cookbook (renderers, chat templates, DPO helpers).
# Requires a Tinker account + TINKER_API_KEY. The app boots and all non-training
# features work without these installed; training/inference need them.
tinker
tinker_cookbook

# DPO / RL loss math
torch>=2.4

# HuggingFace dataset import
datasets>=3.0
huggingface-hub>=0.26
